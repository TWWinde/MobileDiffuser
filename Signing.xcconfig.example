// Copy this file to `Signing.xcconfig` (gitignored) and set your Team ID so you
// can code-sign locally without committing it. Xcode reads it via the project's
// base configuration; the tracked project keeps DEVELOPMENT_TEAM empty.
DEVELOPMENT_TEAM =
CODE_SIGN_STYLE = Automatic
